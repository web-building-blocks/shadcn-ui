---
title: Statistic
description: Displays various statistic components with different variants.
featured: true
component: true
---

## Examples

### Basic Statistic

<ComponentPreview
  name="statistic-demo"
  description="A basic statistic component displaying values with precision."
/>

### Statistic in Card

<ComponentPreview
  name="statistic-incard"
  description="A statistic component displayed within a card, using color-coded indicators."
/>

### Countdown Statistic

<ComponentPreview
  name="statistic-countdown"
  description="A countdown statistic with day-level and second-level display."
/>

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn-ui@latest add statistic
```

```bash
npx shadcn-ui@latest add card
```

```bash
npx shadcn-ui@latest add countdown
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @radix-ui/react-slot
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="statistic" />
<ComponentSource name="card" />
<ComponentSource name="countdown" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { Card } from "@/components/ui/card"
import { Countdown } from "@/components/ui/countdown"
import { Statistic } from "@/components/ui/statistic"
```

### Basic Statistic Example

```tsx
<Statistic title="Active Users" value={112893} />
```

### Statistic in Card Example

```tsx
<Card>
  <Statistic title="Active Users" value={112893} />
</Card>
```

### Countdown Statistic Example

```tsx
<Countdown />
```
